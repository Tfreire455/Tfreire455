<div align="center">

<h1>
  <span class="tm-glitch" data-text="THIAGO MOABI">THIAGO MOABI</span>
</h1>

<p class="tm-sub">
  <span class="tm-type">Software Engineer & Automation Architect</span>
  <span class="tm-sep">•</span>
  <span class="tm-type">AI Integration • LLMs • Autonomous Bots • SaaS</span>
  <span class="tm-sep">•</span>
  <span class="tm-type">Turning manual operations into self-running engines</span>
</p>

<p class="tm-links">
  <a class="tm-btn" href="https://tmdev-one.vercel.app" target="_blank" rel="noreferrer">
    <span class="tm-btn__icon">⟡</span> PORTFOLIO
  </a>
  <a class="tm-btn" href="https://linkedin.com/in/thiagomoabi" target="_blank" rel="noreferrer">
    <span class="tm-btn__icon">↗</span> LINKEDIN
  </a>
  <a class="tm-btn" href="https://github.com/Tfreire455" target="_blank" rel="noreferrer">
    <span class="tm-btn__icon">⌁</span> GITHUB
  </a>
</p>

<div class="tm-divider"></div>

<pre class="tm-code"><code>class ThiagoMoabi {
  readonly brand  = "TM Dev  //  Full systems, AIs and automations";

  readonly stack = {
    frontend : ["Next.js", "React", "TypeScript", "Tailwind CSS", "GSAP"],
    backend  : ["Node.js", "Python", "Express", "Socket.IO"],
    data     : ["PostgreSQL", "Supabase", "Google Sheets API"],
    ai       : ["OpenAI", "Prompt Engineering", "LangChain", "RAG"],
    bots     : ["WhatsApp via Baileys", "Telegram", "Discord.js"],
    infra    : ["Vercel", "Docker", "GitHub Actions", "VPS Linux"],
  };

  readonly mission = "Turn manual operations into autonomous AI-powered engines";
}</code></pre>

<div class="tm-grid">
  <div class="tm-card">
    <div class="tm-card__title">TECH STACK</div>
    <div class="tm-tags">
      <span class="tm-tag">Next.js</span><span class="tm-tag">React</span><span class="tm-tag">TypeScript</span><span class="tm-tag">Tailwind</span>
      <span class="tm-tag">Node.js</span><span class="tm-tag">Python</span><span class="tm-tag">Express</span><span class="tm-tag">Socket.IO</span>
      <span class="tm-tag">PostgreSQL</span><span class="tm-tag">Supabase</span><span class="tm-tag">Sheets API</span>
      <span class="tm-tag">OpenAI</span><span class="tm-tag">LangChain</span><span class="tm-tag">RAG</span>
      <span class="tm-tag">Docker</span><span class="tm-tag">Vercel</span><span class="tm-tag">GitHub Actions</span><span class="tm-tag">Linux</span>
      <span class="tm-tag tm-tag--pulse">GSAP</span><span class="tm-tag tm-tag--pulse">Framer Motion</span>
    </div>
  </div>

  <div class="tm-card">
    <div class="tm-card__title">AI, LLMs & AUTOMATION</div>
    <ul class="tm-list">
      <li><b>Autonomous Bots:</b> WhatsApp (Baileys) engine 24/7, campanhas com delays humanizados + anti-spam, tracking em PostgreSQL.</li>
      <li><b>LLMs & Prompt Engineering:</b> copy dinâmica por SKU (nome, preço, coleção, estoque), modos Normal/Blitz/Collection via dashboard.</li>
      <li><b>SaaS Automation:</b> planilha → DB → LLM → campanhas, CRM com lead capture, aniversariantes VIP, cupom automático via cron.</li>
      <li><b>Resilience:</b> retries com backoff, fallbacks de mídia, loops canceláveis, padrões anti memory-leak.</li>
    </ul>
  </div>

  <div class="tm-card tm-wide">
    <div class="tm-card__title">CASE STUDY — SOLINE SALES OS</div>
    <p class="tm-quote">
      AI analisa cada SKU em estoque e escreve copy única. O bot dispara campanhas no horário. O dashboard acompanha tudo em tempo real.
      <span class="tm-quote__accent">Zero esforço manual, do início ao fim.</span>
    </p>
    <div class="tm-metrics">
      <div class="tm-metric">
        <div class="tm-metric__num tm-counter" data-to="24">0</div>
        <div class="tm-metric__label">h/7 bot uptime</div>
      </div>
      <div class="tm-metric">
        <div class="tm-metric__num tm-counter" data-to="3">0</div>
        <div class="tm-metric__label">AI modes</div>
      </div>
      <div class="tm-metric">
        <div class="tm-metric__num tm-counter" data-to="0">0</div>
        <div class="tm-metric__label">manual ops</div>
      </div>
      <div class="tm-metric">
        <div class="tm-metric__num tm-counter" data-to="100">0</div>
        <div class="tm-metric__label">% automated</div>
      </div>
    </div>
  </div>

  <div class="tm-card">
    <div class="tm-card__title">FEATURED PROJECTS</div>
    <div class="tm-projects">
      <a class="tm-project" href="https://github.com/Tfreire455/soline-catalogo-cta" target="_blank" rel="noreferrer">
        <div class="tm-project__name">Soline Digital Catalog</div>
        <div class="tm-project__stack">React • Tailwind • GSAP • Sheets API</div>
        <div class="tm-project__desc">High-performance SPA with Google Sheets as Headless CMS.</div>
      </a>

      <a class="tm-project" href="https://github.com/Tfreire455/bot-vendas-soline" target="_blank" rel="noreferrer">
        <div class="tm-project__name">Soline Sales OS</div>
        <div class="tm-project__stack">Node.js • Baileys • OpenAI • PostgreSQL</div>
        <div class="tm-project__desc">Autonomous B2B sales engine for WhatsApp with AI copywriting.</div>
      </a>
    </div>
  </div>

  <div class="tm-card">
    <div class="tm-card__title">STATS (TEXT MODE)</div>
    <div class="tm-stats">
      <div class="tm-stat"><span class="tm-dot"></span> Focus: Systems, AI, Automations</div>
      <div class="tm-stat"><span class="tm-dot"></span> Delivery: Production-ready, end-to-end</div>
      <div class="tm-stat"><span class="tm-dot"></span> Architecture: Resilient, observable, scalable</div>
      <div class="tm-stat"><span class="tm-dot"></span> Style: Cinematic UI, tech aesthetic</div>
    </div>
  </div>
</div>

<div align="center" class="tm-footer">
  <div class="tm-footer__line">
    <span class="tm-marquee">
      <span>Thanks for visiting. Let's build something together. • </span>
      <span>Thanks for visiting. Let's build something together. • </span>
      <span>Thanks for visiting. Let's build something together. • </span>
    </span>
  </div>

  <div class="tm-sign">
    <b>Thiago Moabi — TM Dev</b><br/>
    <sub>Full systems, AIs and automations</sub>
  </div>
</div>

<!-- =========================
     2D ANIMATIONS + TECH FONTS
     No external images used.
     Works in GitHub README.
========================== -->
<style>
  /* ====== Tech font stack (no imports; GitHub blocks @import) ====== */
  :root{
    --bg0:#0a0a0f;
    --bg1:#0d1117;
    --bg2:#111827;
    --line:#1F6FEB;
    --text:#e6edf3;
    --muted:#9aa4b2;
    --gold:#F8D866;
    --glass: rgba(255,255,255,.06);
    --glass2: rgba(255,255,255,.08);
    --shadow: rgba(0,0,0,.35);
  }

  /* ====== Animated "header" feel via gradients ====== */
  .tm-divider{
    width: min(980px, 96%);
    height: 2px;
    margin: 22px auto 18px;
    background: linear-gradient(90deg, transparent, var(--line), var(--gold), var(--line), transparent);
    filter: drop-shadow(0 0 10px rgba(31,111,235,.35));
    position: relative;
    overflow:hidden;
  }
  .tm-divider::after{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(90deg, transparent 0%, rgba(255,255,255,.35) 50%, transparent 100%);
    transform: translateX(-120%);
    animation: tm-scan 3.4s linear infinite;
    opacity:.35;
  }

  /* ====== Headline glitch + neon ====== */
  .tm-glitch{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-weight: 900;
    font-size: clamp(28px, 4.1vw, 46px);
    letter-spacing: .22em;
    color: var(--text);
    text-transform: uppercase;
    position: relative;
    display: inline-block;
    padding: 10px 14px;
    margin: 10px 0 6px;
    text-shadow:
      0 0 10px rgba(31,111,235,.28),
      0 0 24px rgba(248,216,102,.12);
  }
  .tm-glitch::before,
  .tm-glitch::after{
    content: attr(data-text);
    position:absolute;
    left:0;
    top:0;
    width:100%;
    overflow:hidden;
    opacity:.9;
    pointer-events:none;
  }
  .tm-glitch::before{
    transform: translate(1px, 0);
    color: rgba(31,111,235,.85);
    clip-path: inset(0 0 60% 0);
    animation: tm-glitch 2.6s infinite linear alternate-reverse;
  }
  .tm-glitch::after{
    transform: translate(-1px, 0);
    color: rgba(248,216,102,.75);
    clip-path: inset(55% 0 0 0);
    animation: tm-glitch 2.1s infinite linear alternate;
  }

  /* ====== Subtitle typewriter (CSS-only) ====== */
  .tm-sub{
    width: min(980px, 94%);
    margin: 0 auto 18px;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    color: var(--muted);
    line-height: 1.7;
    font-size: 13px;
  }
  .tm-type{
    display:inline-block;
    white-space:nowrap;
    overflow:hidden;
    border-right: 1px solid rgba(31,111,235,.7);
    padding-right: 6px;
    margin: 2px 0;
    animation: tm-type 5.6s steps(40, end) infinite, tm-caret .85s step-end infinite;
    max-width: 0;
  }
  .tm-type:nth-child(1){ animation-delay: 0s, 0s; }
  .tm-type:nth-child(3){ animation-delay: 1.6s, 1.6s; }
  .tm-type:nth-child(5){ animation-delay: 3.2s, 3.2s; }
  .tm-sep{ margin: 0 10px; opacity:.35; }

  /* ====== Buttons ====== */
  .tm-links{
    display:flex;
    gap:10px;
    justify-content:center;
    flex-wrap:wrap;
    margin: 8px auto 6px;
  }
  .tm-btn{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    text-decoration:none !important;
    color: var(--text) !important;
    border: 1px solid rgba(31,111,235,.35);
    background: linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
    padding: 10px 14px;
    border-radius: 14px;
    box-shadow: 0 10px 30px var(--shadow);
    position: relative;
    overflow:hidden;
    letter-spacing: .12em;
    font-size: 12px;
    display:inline-flex;
    align-items:center;
    gap:10px;
    transform: translateZ(0);
    transition: transform .22s ease, border-color .22s ease, filter .22s ease;
  }
  .tm-btn:hover{
    transform: translateY(-2px);
    border-color: rgba(248,216,102,.55);
    filter: drop-shadow(0 0 16px rgba(31,111,235,.18));
  }
  .tm-btn::after{
    content:"";
    position:absolute;
    inset:-60% -80%;
    background: radial-gradient(circle at 30% 30%, rgba(31,111,235,.22), transparent 45%),
                radial-gradient(circle at 70% 70%, rgba(248,216,102,.14), transparent 50%);
    animation: tm-float 5.5s ease-in-out infinite;
    opacity:.8;
  }
  .tm-btn__icon{
    opacity:.9;
    filter: drop-shadow(0 0 10px rgba(31,111,235,.2));
  }

  /* ====== Code block styling ====== */
  .tm-code{
    width: min(980px, 96%);
    margin: 16px auto 18px;
    padding: 14px 16px;
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,.09);
    background: linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
    box-shadow: 0 16px 40px rgba(0,0,0,.25);
    position: relative;
    overflow:hidden;
    text-align:left;
  }
  .tm-code::before{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(90deg, transparent, rgba(31,111,235,.18), transparent);
    transform: translateX(-120%);
    animation: tm-scan 4.2s linear infinite;
    opacity:.45;
  }
  .tm-code code{
    display:block;
    color: #c9d1d9;
    font-size: 12px;
    line-height: 1.55;
    white-space: pre;
  }

  /* ====== Layout grid ====== */
  .tm-grid{
    width: min(980px, 96%);
    margin: 0 auto;
    display:grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }
  @media (max-width: 880px){
    .tm-grid{ grid-template-columns: 1fr; }
    .tm-sep{ display:none; }
    .tm-type{ display:block; }
  }

  .tm-card{
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,.09);
    background: linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
    box-shadow: 0 16px 40px rgba(0,0,0,.25);
    padding: 14px 14px 12px;
    position: relative;
    overflow:hidden;
  }
  .tm-card::after{
    content:"";
    position:absolute;
    inset:-40%;
    background:
      radial-gradient(circle at 25% 30%, rgba(31,111,235,.14), transparent 45%),
      radial-gradient(circle at 75% 70%, rgba(248,216,102,.10), transparent 50%);
    animation: tm-float 6.3s ease-in-out infinite;
    opacity:.9;
    pointer-events:none;
  }
  .tm-wide{ grid-column: 1 / -1; }

  .tm-card__title{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    color: var(--text);
    letter-spacing: .22em;
    font-size: 12px;
    text-transform: uppercase;
    margin-bottom: 10px;
    display:flex;
    align-items:center;
    gap:10px;
  }
  .tm-card__title::before{
    content:"";
    width: 10px;
    height: 10px;
    border-radius: 3px;
    background: linear-gradient(180deg, var(--line), var(--gold));
    box-shadow: 0 0 18px rgba(31,111,235,.25);
    animation: tm-pulse 2.2s ease-in-out infinite;
  }

  /* ====== Tags ====== */
  .tm-tags{ display:flex; flex-wrap:wrap; gap:8px; }
  .tm-tag{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 11px;
    color: var(--text);
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid rgba(31,111,235,.28);
    background: rgba(13,17,23,.35);
    backdrop-filter: blur(6px);
    transform: translateZ(0);
    position: relative;
    overflow:hidden;
  }
  .tm-tag::after{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.12), transparent);
    transform: translateX(-120%);
    animation: tm-scan 5.4s linear infinite;
    opacity:.55;
  }
  .tm-tag--pulse{
    border-color: rgba(248,216,102,.4);
    animation: tm-pulse 2.6s ease-in-out infinite;
  }

  /* ====== Lists ====== */
  .tm-list{
    margin: 0;
    padding-left: 16px;
    color: var(--muted);
    font-size: 12.5px;
    line-height: 1.65;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  }
  .tm-list b{ color: var(--text); }

  /* ====== Quote ====== */
  .tm-quote{
    margin: 0 0 12px;
    color: var(--muted);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 12.5px;
    line-height: 1.7;
  }
  .tm-quote__accent{
    display:block;
    margin-top: 6px;
    color: var(--text);
    letter-spacing: .08em;
  }

  /* ====== Metrics + counters ====== */
  .tm-metrics{
    display:grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
    margin-top: 10px;
  }
  @media (max-width: 880px){
    .tm-metrics{ grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }
  .tm-metric{
    border-radius: 16px;
    border: 1px solid rgba(255,255,255,.09);
    background: rgba(13,17,23,.28);
    padding: 12px;
    text-align:center;
    position: relative;
    overflow:hidden;
  }
  .tm-metric::after{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(90deg, transparent, rgba(31,111,235,.14), transparent);
    transform: translateX(-120%);
    animation: tm-scan 3.8s linear infinite;
    opacity:.5;
  }
  .tm-metric__num{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 22px;
    color: var(--text);
    letter-spacing: .12em;
    text-shadow: 0 0 16px rgba(31,111,235,.18);
  }
  .tm-metric__label{
    margin-top: 6px;
    font-size: 11px;
    color: var(--muted);
    letter-spacing: .18em;
    text-transform: uppercase;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
  }

  /* ====== Projects ====== */
  .tm-projects{ display:grid; gap:10px; }
  .tm-project{
    display:block;
    text-decoration:none !important;
    border-radius: 16px;
    border: 1px solid rgba(31,111,235,.28);
    background: rgba(13,17,23,.28);
    padding: 12px 12px;
    transition: transform .2s ease, border-color .2s ease, filter .2s ease;
    position: relative;
    overflow:hidden;
  }
  .tm-project:hover{
    transform: translateY(-2px);
    border-color: rgba(248,216,102,.5);
    filter: drop-shadow(0 0 16px rgba(31,111,235,.14));
  }
  .tm-project::after{
    content:"";
    position:absolute;
    inset:-50%;
    background: radial-gradient(circle at 30% 30%, rgba(31,111,235,.14), transparent 45%),
                radial-gradient(circle at 70% 70%, rgba(248,216,102,.10), transparent 50%);
    animation: tm-float 7.2s ease-in-out infinite;
    opacity:.85;
  }
  .tm-project__name{
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    color: var(--text) !important;
    letter-spacing:.12em;
    font-weight: 800;
    font-size: 12.5px;
    text-transform: uppercase;
  }
  .tm-project__stack{
    margin-top: 6px;
    color: rgba(31,111,235,.95);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 11px;
    letter-spacing:.12em;
  }
  .tm-project__desc{
    margin-top: 6px;
    color: var(--muted);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 12px;
    line-height: 1.55;
  }

  /* ====== Stats (text) ====== */
  .tm-stats{
    display:grid;
    gap: 8px;
    color: var(--muted);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 12.5px;
  }
  .tm-stat{
    display:flex;
    align-items:center;
    gap:10px;
    padding: 8px 10px;
    border-radius: 14px;
    border: 1px solid rgba(255,255,255,.08);
    background: rgba(13,17,23,.24);
    position: relative;
    overflow:hidden;
  }
  .tm-stat::after{
    content:"";
    position:absolute;
    inset:0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.10), transparent);
    transform: translateX(-120%);
    animation: tm-scan 6.2s linear infinite;
    opacity:.55;
  }
  .tm-dot{
    width: 8px;
    height: 8px;
    border-radius: 3px;
    background: linear-gradient(180deg, var(--line), var(--gold));
    box-shadow: 0 0 16px rgba(31,111,235,.18);
    animation: tm-pulse 2.1s ease-in-out infinite;
    flex: 0 0 auto;
  }

  /* ====== Footer marquee ====== */
  .tm-footer{
    width: min(980px, 96%);
    margin: 16px auto 0;
  }
  .tm-footer__line{
    border-radius: 16px;
    border: 1px solid rgba(255,255,255,.09);
    background: rgba(13,17,23,.22);
    overflow:hidden;
    padding: 10px 0;
    position: relative;
  }
  .tm-marquee{
    display:inline-flex;
    gap: 18px;
    white-space:nowrap;
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 11px;
    color: rgba(230,237,243,.65);
    letter-spacing: .18em;
    animation: tm-marquee 18s linear infinite;
    padding-left: 100%;
  }
  .tm-sign{
    margin-top: 10px;
    color: rgba(230,237,243,.85);
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    letter-spacing: .10em;
    line-height: 1.55;
  }
  .tm-sign sub{ color: rgba(154,164,178,.9); letter-spacing: .18em; }

  /* ====== Keyframes ====== */
  @keyframes tm-scan{
    0%{ transform: translateX(-120%); }
    100%{ transform: translateX(120%); }
  }
  @keyframes tm-float{
    0%,100%{ transform: translate(0,0) rotate(0deg); }
    50%{ transform: translate(10px,-8px) rotate(2deg); }
  }
  @keyframes tm-pulse{
    0%,100%{ transform: scale(1); filter: brightness(1); }
    50%{ transform: scale(1.06); filter: brightness(1.2); }
  }
  @keyframes tm-glitch{
    0%{ clip-path: inset(0 0 60% 0); transform: translate(1px,0); }
    20%{ clip-path: inset(10% 0 35% 0); transform: translate(2px,-1px); }
    40%{ clip-path: inset(25% 0 45% 0); transform: translate(0px,1px); }
    60%{ clip-path: inset(5% 0 65% 0); transform: translate(2px,0px); }
    80%{ clip-path: inset(18% 0 30% 0); transform: translate(1px,-1px); }
    100%{ clip-path: inset(0 0 60% 0); transform: translate(0,0); }
  }
  @keyframes tm-type{
    0%{ max-width:0; }
    35%{ max-width: 980px; }
    70%{ max-width: 980px; }
    100%{ max-width:0; }
  }
  @keyframes tm-caret{
    0%, 100%{ border-color: rgba(31,111,235,.0); }
    50%{ border-color: rgba(31,111,235,.75); }
  }
  @keyframes tm-marquee{
    0%{ transform: translateX(0); }
    100%{ transform: translateX(-100%); }
  }
</style>

<script>
/* ==========================================
   Optional JS micro-animations (GitHub may strip scripts).
   If scripts are blocked, everything still works via CSS.
========================================== */
(() => {
  try{
    const counters = document.querySelectorAll(".tm-counter");
    const preferReduced = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if(preferReduced) return;

    counters.forEach(el => {
      const to = Number(el.getAttribute("data-to") || "0");
      const duration = 900;
      const start = performance.now();
      const from = 0;

      function tick(now){
        const p = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - p, 3);
        const val = Math.round(from + (to - from) * eased);
        el.textContent = String(val);
        if(p < 1) requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    });
  }catch(e){}
})();
</script>
